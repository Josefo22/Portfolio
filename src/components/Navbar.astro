<!-- Navbar.astro -->
<nav class="fixed top-0 left-0 right-0 z-50 bg-white/80 dark:bg-gray-900/80 backdrop-blur-md shadow-sm transition-all duration-300" id="main-nav">
    <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between items-center h-16">
        <!-- Logo -->
        <div class="flex-shrink-0 flex items-center">
          <a href="#" class="text-xl font-bold text-emerald-600 dark:text-emerald-400">
            <span class="inline-block">JJ</span>
            <span class="ml-1 text-gray-800 dark:text-white">Dev</span>
          </a>
        </div>
        
        <!-- Menú versión escritorio -->
        <div class="hidden md:block">
          <div class="ml-4 flex items-center space-x-4">
            <a href="#hero-section" class="nav-link text-gray-600 hover:text-emerald-600 dark:text-gray-300 dark:hover:text-emerald-400 px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200" data-i18n="home">Inicio</a>
            <a href="#about" class="nav-link text-gray-600 hover:text-emerald-600 dark:text-gray-300 dark:hover:text-emerald-400 px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200" data-i18n="about">Sobre mí</a>
            <a href="#skills" class="nav-link text-gray-600 hover:text-emerald-600 dark:text-gray-300 dark:hover:text-emerald-400 px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200" data-i18n="skills">Habilidades</a>
            <a href="#projects" class="nav-link text-gray-600 hover:text-emerald-600 dark:text-gray-300 dark:hover:text-emerald-400 px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200" data-i18n="projects">Proyectos</a>
            <a href="#contact" class="nav-link bg-emerald-500 hover:bg-emerald-600 text-white px-4 py-2 rounded-md text-sm font-medium transition-colors duration-200" data-i18n="contact">Contacto</a>
          </div>
        </div>
      
        
        <!-- Botón menú móvil -->
        <div class="flex md:hidden">
          <button onclick="toggleMobileMenu()" type="button" class="inline-flex items-center justify-center p-2 rounded-md text-gray-600 hover:text-emerald-600 hover:bg-gray-100 dark:text-gray-300 dark:hover:text-emerald-400 dark:hover:bg-gray-800 transition-colors duration-200">
            <span class="sr-only">Abrir menú</span>
            <!-- Iconos de menú -->
            <svg id="menu-open-icon" xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
            </svg>
            <svg id="menu-close-icon" xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 hidden" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
            </svg>
          </button>
        </div>
      </div>
    </div>
  
    <!-- Menú móvil -->
    <div id="mobile-menu" class="md:hidden hidden bg-white dark:bg-gray-900 shadow-lg w-full transition-all duration-200">
      <div class="px-4 py-3 space-y-2 border-t border-gray-200 dark:border-gray-700">
        <a href="javascript:void(0)" onclick="navigateToSection('#hero-section')" class="block px-4 py-2 rounded-lg text-base font-medium text-gray-600 hover:text-emerald-600 hover:bg-gray-100 dark:text-gray-300 dark:hover:text-emerald-400 dark:hover:bg-gray-800 transition-colors duration-200" data-i18n="home">Inicio</a>
        <a href="javascript:void(0)" onclick="navigateToSection('#about')" class="block px-4 py-2 rounded-lg text-base font-medium text-gray-600 hover:text-emerald-600 hover:bg-gray-100 dark:text-gray-300 dark:hover:text-emerald-400 dark:hover:bg-gray-800 transition-colors duration-200" data-i18n="about">Sobre mí</a>
        <a href="javascript:void(0)" onclick="navigateToSection('#skills')" class="block px-4 py-2 rounded-lg text-base font-medium text-gray-600 hover:text-emerald-600 hover:bg-gray-100 dark:text-gray-300 dark:hover:text-emerald-400 dark:hover:bg-gray-800 transition-colors duration-200" data-i18n="skills">Habilidades</a>
        <a href="javascript:void(0)" onclick="navigateToSection('#projects')" class="block px-4 py-2 rounded-lg text-base font-medium text-gray-600 hover:text-emerald-600 hover:bg-gray-100 dark:text-gray-300 dark:hover:text-emerald-400 dark:hover:bg-gray-800 transition-colors duration-200" data-i18n="projects">Proyectos</a>
        <a href="javascript:void(0)" onclick="navigateToSection('#contact')" class="block px-4 py-2 rounded-lg text-base font-medium bg-emerald-500 hover:bg-emerald-600 text-white hover:text-white transition-colors duration-200 mt-4" data-i18n="contact">Contacto</a>
      </div>
    </div>
  </nav>
  
  <script is:inline>
    // Función global para alternar el menú móvil
    function toggleMobileMenu() {
      const menu = document.getElementById('mobile-menu');
      const openIcon = document.getElementById('menu-open-icon');
      const closeIcon = document.getElementById('menu-close-icon');
      
      if (menu) {
        menu.classList.toggle('hidden');
        openIcon.classList.toggle('hidden');
        closeIcon.classList.toggle('hidden');
        console.log("Menú móvil toggled");
      }
    }
    
    // Función global para cerrar el menú
    function closeMenu() {
      const menu = document.getElementById('mobile-menu');
      const openIcon = document.getElementById('menu-open-icon');
      const closeIcon = document.getElementById('menu-close-icon');
      
      if (menu) {
        menu.classList.add('hidden');
        openIcon.classList.remove('hidden');
        closeIcon.classList.add('hidden');
        console.log("Menú móvil cerrado");
      }
    }

    // Función global para navegar a una sección
    function navigateToSection(sectionId) {
      closeMenu();
      
      setTimeout(() => {
        const section = document.querySelector(sectionId);
        if (section) {
          window.scrollTo({
            top: section.offsetTop - 70,
            behavior: 'smooth'
          });
        }
      }, 300);
    }

    // Inicialización y configuración adicional al cargar la página
    document.addEventListener('DOMContentLoaded', () => {
      // Cambiar estilo del navbar al hacer scroll
      const navbar = document.getElementById('main-nav');
      if (navbar) {
        window.addEventListener('scroll', () => {
          if (window.scrollY > 10) {
            navbar.classList.add('shadow-md', 'bg-white/95', 'dark:bg-gray-900/95');
            navbar.classList.remove('bg-white/80', 'dark:bg-gray-900/80');
          } else {
            navbar.classList.remove('shadow-md', 'bg-white/95', 'dark:bg-gray-900/95');
            navbar.classList.add('bg-white/80', 'dark:bg-gray-900/80');
          }
        });
      }
      
      // Resaltar enlace activo según la sección visible
      const sections = document.querySelectorAll('section[id]');
      const navLinks = document.querySelectorAll('.nav-link');
      
      window.addEventListener('scroll', () => {
        let current = '';
        
        sections.forEach(section => {
          const sectionTop = section.offsetTop - 100;
          const sectionHeight = section.offsetHeight;
          const sectionId = section.getAttribute('id') || '';
          
          if (window.scrollY >= sectionTop && window.scrollY < sectionTop + sectionHeight) {
            current = sectionId;
          }
        });
        
        navLinks.forEach(link => {
          link.classList.remove('text-emerald-600', 'dark:text-emerald-400');
          
          const href = link.getAttribute('href');
          if (href === `#${current}`) {
            link.classList.add('text-emerald-600', 'dark:text-emerald-400');
          }
        });
      });
    });
  </script>

<style>
  /* Estilos mejorados para el menú móvil */
  #mobile-menu {
    width: 100%;
    z-index: 50;
    border-top: 1px solid rgba(229, 231, 235, 0.5);
  }
  
  #mobile-menu.hidden {
    display: none;
  }
  
  /* Específico para dispositivos táctiles */
  @media (max-width: 768px) {
    #mobile-menu a, button {
      min-height: 50px; /* Mayor área táctil */
      display: block;
      width: 100%;
    }
    
    /* Prevenir que la pantalla rebote en iOS pero sin bloquear el scroll */
    html, body {
      -webkit-overflow-scrolling: touch;
      overflow-x: hidden;
    }
  }
</style>